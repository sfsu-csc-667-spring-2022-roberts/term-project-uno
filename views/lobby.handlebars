<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/views/lobby.css" />
    <link rel="stylesheet" type="text/css" href="/css/partials/navbar.css" />
    <link rel="stylesheet" type="text/css" href="/css/scrollbar.css">
    <script defer type="text/javascript" src="/js/views/lobby.js"></script>
    <title>{{title}}</title>
  </head>
  <body>
    {{> navbar}}
    <main>
      <div id="content-container">
        <div id="lobby-container">
          <div id="lobby-members-list">
            <table>
              <tbody>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24444</td>
                  <td class="lobby-guest-status">Host</td>
                  <td>
                  </td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24</td>
                  <td class="lobby-guest-status">Ready</td>
                  <td>
                    <button class="lobby-button">Kick</button>
                  </td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24</td>
                  <td class="lobby-guest-status">Ready</td>
                  <td>
                    <button class="lobby-button">Kick</button>
                  </td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24</td>
                  <td class="lobby-guest-status">Ready</td>
                  <td>
                    <button class="lobby-button">Kick</button>
                  </td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24</td>
                  <td class="lobby-guest-status">Ready</td>
                  <td>
                    <button class="lobby-button">Kick</button>
                  </td>
                </tr>
              </tbody>
            </table>
            <table>
            <tbody>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24444</td>
                  <td class="lobby-guest-status">Not Ready</td>
                  <td>
                    <button class="lobby-button">Kick</button>
                  </td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24</td>
                  <td class="lobby-guest-status">Ready</td>
                  <td>
                    <button class="lobby-button">Kick</button>
                  </td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                    <div class="lobby-guest-icon">
                      {{#if user.pictureUrl}}
                      <img src="{{user.pictureUrl}}">
                      {{else}}
                      <img src="/images/default-profile-pic.png">
                      {{/if}}
                    </div>
                  </td>
                  <td class="lobby-guest-name">stephenjusto24</td>
                  <td class="lobby-guest-status">Ready</td>
                  <td>
                    <button class="lobby-button">Kick</button>
                  </td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                  </td>
                  <td class="lobby-guest-name"></td>
                  <td class="lobby-guest-status"></td>
                  <td></td>
                </tr>
                <tr class="lobby-guest">
                  <td class="lobby-guest-icon-col">
                  </td>
                  <td class="lobby-guest-name"></td>
                  <td class="lobby-guest-status"></td>
                  <td></td>
                </tr>
              </tbody>
            </table>
          </div>
          <div id="lobby-menu">
            <div id="invitation-container">
              <span id="invite-error">TESTING</span>
              <form>
                <input type="text" placeholder="Username" id="username" name="username">
                <button class="lobby-button">Invite</button>
              </form>
            </div>
            <div id="lobby-options">
              <button class="lobby-button">Start</button>
              <button class="lobby-button">Leave Lobby</button>
            </div>
          </div>
        </div>
        <div id="chat-container">
          <div id="messages">
            <div class="message-container">
              <strong class="message-sender">stephenjusto24</strong>
              <div class="message">A long sample message here a long sample message here a long sample message here a long sample message here a long sample message here a long sample message here</div>
              <span class="message-time">4:20 PM</span>
            </div>
            <div class="message-container">
              <strong class="message-sender">stephenjusto24</strong>
              <div class="message">A long sample message here a long sample message here a long sample message here a long sample message here a long sample message here a long sample message here</div>
              <span class="message-time">4:20 PM</span>
            </div>
          </div>
          <textarea id="message-input" placeholder="Type message..."></textarea>
        </div>
      </div>
    </main>
  </body>
</html>
